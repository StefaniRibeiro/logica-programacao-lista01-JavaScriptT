function areaquadrado(){
    var lado=parseInt(prompt("Informe o valor do comprimento do quadrado"));
    var areaquadrado= Math.pow(lado,2);
    prompt("A área do quadrado é igual a: "+areaquadrado);
}