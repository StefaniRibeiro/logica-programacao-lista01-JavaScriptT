function areacirculo(){
    var raio=parseInt(prompt("Informe o valor do raio do círculo: "));
    var areacirculo=Math.PI*Math.pow(raio,2);
    prompt("A área do círculo é igual a: "+areacirculo);
    }